name: booky_camera_app
description: Android app to take picture of books, group them by bundle, add the weight and state metadata

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0-305.0.dev <4.0.0'

dependencies:
  camera: ^0.10.3+2
  flutter:
    sdk: flutter
  gallery_saver: ^2.3.2
  json_annotation: ^4.8.0
  path: ^1.8.3
  permission_handler: ^10.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  json_serializable: ^6.6.1
  build_runner: ^2.4.1

flutter:
  uses-material-design: true